# Example environment variables for local workflow testing
# Copy this file to workflow.env and fill with your values
# Usage: docker run --env-file workflow.env my-workflow:latest

# Workflow Configuration
WORKFLOW_ID=test-workflow-123
NODE_ENV=development
PORT=8080

# Secrets (example - replace with real values)
# Resend API Key (for send-email node)
SECRET_resend_api_key=re_xxxxxxxxxxxxx

# Stripe API Key (example)
# SECRET_stripe_key=sk_test_xxxxxxxxxxxxx

# Custom API Keys (example)
# SECRET_custom_api_key=your_api_key_here

# Notes:
# - All secrets must be prefixed with SECRET_
# - Variable names are case-sensitive
# - Do not commit this file with real secrets to version control
# - In production, use Kubernetes Secrets instead
